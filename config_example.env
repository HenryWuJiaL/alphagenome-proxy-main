# AlphaGenome Communication Proxy 配置文件示例
# 复制此文件为 .env 并根据实际情况修改

# JSON 服务的基础 URL
# 本地开发时使用本地服务
JSON_SERVICE_BASE_URL=http://127.0.0.1:8000

# 对接官方 AlphaGenome 云服务时使用
# JSON_SERVICE_BASE_URL=https://api.alphagenome.google.com

# AlphaGenome API Key (从官方申请获得)
# 如果不需要 API key，可以留空
ALPHAGENOME_API_KEY=your_api_key_here

# API Key 的请求头名称 (可选，默认为 Authorization)
API_KEY_HEADER=Authorization

# API Key 的前缀 (可选，默认为 "Bearer ")
# 如果使用 Authorization header，通常使用 "Bearer " 前缀
# 如果使用自定义 header，可以设置为空字符串 ""
API_KEY_PREFIX=Bearer 

# 日志级别 (可选)
# LOG_LEVEL=INFO 